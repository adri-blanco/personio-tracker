{
  "name": "personio-timesheet-auto-filler",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that automates filling missing period entries flagged by an alert icon on a Personio timesheet page.",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/test/**/*.test.js"]
  }
}
